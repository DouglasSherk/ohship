<% if @package.shipper_tracking.nil? %>
  Thank you! We will notify you when the shipper has shipped your package.<br />
  If you do not receive an update with in 1-2 business days, please email us at
  <%= mail_to Mailer::EMAIL %>
<% else %>
  Your package is en route! The tracking number is: <%= @package.shipper_tracking %> (<%= @package.shipper_tracking_carrier %>)<br />
  Please notify us when you receive the package.<br />

  <br />
  The actual shipping cost came to <strong><%= number_to_currency @package.shipping_estimate %></strong>. Please contact
  us at <%= mail_to Mailer::EMAIL %> if this is incorrect (see receipt).<br />

  <br />

  <%= form_tag package_path + '/shippee_action' do %>
    <div class="buttons">
      <a href="<%= photo_path @receipt %>" target="_blank" class="btn btn-default">View shipping receipt</a>
      <button type="submit" name="submit" value="received" class="btn btn-primary">I received the package</button>
    </div>
  <% end %>
<% end %>
