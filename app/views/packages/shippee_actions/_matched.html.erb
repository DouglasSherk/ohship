<% if @package.shippee_tracking.nil? %>
  You can now send your package to the shipper's address:
  <address>
    <strong><%= @package.shipper.name %></strong><br />
    <%= @package.shipper.address %><br />
    <%= @package.shipper.city %>, <%= @package.shipper.state%>, <%= @package.shipper.country %><br />
    <%= @package.shipper.postal_code %>
  </address>
  When the package ships, please enter the tracking information here, if possible.
  <%= form_tag package_path + '/shippee_action', :class => 'form-inline' do %>
    <div class="form-group">
      <%= text_field_tag :tracking_number, '', :class => 'form-control', :placeholder => 'Tracking number' %>
    </div>
    <div class="form-group">
      <%= text_field_tag :tracking_carrier, '', :class => 'form-control', :placeholder => 'Carrier' %>
    </div>
    <div class="buttons">
      <button type="submit" name="submit" value="shipped" class="btn btn-primary">Package has shipped</button>
    </div>
  <% end %>
<% else %>
  Thanks! We will notify you when the shipper receives the item.
  <% if @package.shippee_tracking.empty? %>
    No tracking number was provided.
  <% else %>
    Your tracking number was: <%= @package.shippee_tracking %> (<%= @package.shippee_tracking_carrier %>).
  <% end %>
  <br />
  If the shipper does not respond soon after tracking indicates the package has been received, please email us at
  <%= link_to 'hello@ohship.me', 'mailto:hello@ohship.me' %>.
<% end %>
