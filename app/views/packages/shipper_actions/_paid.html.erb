<% if @package.shipper_tracking.nil? %>
  Payment received. Please send the package to the following address:
  <address>
    <strong><%= @package.ship_to_name %></strong><br />
    <%= @package.ship_to_address %><br />
    <%= @package.ship_to_city %>, <%= @package.ship_to_state%>, <%= @package.ship_to_country %><br />
    <%= @package.ship_to_postal_code %>
  </address>

  Use <strong><%= Package::SHIPPING_CLASSES[@package.shipping_class] %></strong> shipping; the cost should be about <%= number_to_currency @package.shipping_estimate %>.<br />
  Please upload the shipping receipt and tracking information for reimbursement.

  <%= form_tag package_path + '/shipper_action', :multipart => true, :class => 'form-horizontal col-md-12' do %>
    <div class="form-group">
      <%= label_tag :receipt_upload, 'Upload receipt', :class => 'col-md-2 control-label' %>
      <div class="col-md-10">
        <%= file_field_tag :receipt_upload, :class => 'form-control' %>
      </div>
    </div>
    <div class="form-group">
      <%= label_tag :shipping_cost, 'Actual shipping cost', :class => 'col-md-2 control-label' %>
      <div class="col-md-10">
        <%= text_field_tag :shipping_cost, flash[:shipping_cost], :class => 'form-control' %>
      </div>
    </div>
    <div class="form-group">
      <%= label_tag :tracking_number, 'Tracking #', :class => 'col-md-2 control-label' %>
      <div class="col-md-10">
        <%= text_field_tag :tracking_number, flash[:tracking_number], :class => 'form-control' %>
      </div>
    </div>
    <div class="form-group">
      <%= label_tag :tracking_carrier, 'Tracking carrier', :class => 'col-md-2 control-label' %>
      <div class="col-md-10">
        <%= text_field_tag :tracking_carrier, flash[:tracking_carrier], :class => 'form-control' %>
      </div>
    </div>

    <div class="form-group buttons">
      <% # TODO: add confirmation %>
      <button type="submit" name="submit" value="submit" class="btn btn-primary">Submit</button>
    </div>
  <% end %>
<% else %>
  Thank you! The package is en route to the receiver. The tracking number is: <%= @package.shipper_tracking %> (<%= @package.shipper_tracking_carrier %>)<br />
  You should receive your payment shortly.
<% end %>
