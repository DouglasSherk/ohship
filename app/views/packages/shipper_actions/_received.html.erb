<% if @package.shipping_estimate_confirmed %>
  Thank you! The estimated shipping cost will be <%= number_to_currency @package.shipping_estimate %>.<br />
  You will be notified when the user makes the required payment.
<% elsif @package.shipping_estimate %>
  The anticipated cost of shipping is <strong><%= number_to_currency @package.shipping_estimate %></strong>.
  Does that look correct?
  <%= form_tag package_path + '/shipper_action', :class => 'form-horizontal' do %>
    <div class="buttons">
      <button type="submit" name="submit" value="accept" class="btn btn-primary">Accept</button>
      <button type="submit" name="submit" value="back" class="btn btn-danger">Back and edit details</button>
    </div>
  <% end %>
<% elsif @package.shipping_estimate.nil? %>
  Please measure and weigh the package to provide an accurate estimate of the shipping cost.
  <%= form_tag package_path + '/shipper_action', :class => 'form-horizontal' do %>
    <div class="col-md-12">
      <div class="form-group">
        <%= label_tag :length_in, 'Length (in)', :class => 'col-md-2 control-label' %>
        <div class="col-md-10">
          <%= text_field_tag :length_in, '', :class => 'form-control' %>
        </div>
      </div>
      <div class="form-group">
        <%= label_tag :width_in, 'Width (in)', :class => 'col-md-2 control-label' %>
        <div class="col-md-10">
          <%= text_field_tag :width_in, '', :class => 'form-control' %>
        </div>
      </div>
      <div class="form-group">
        <%= label_tag :height_in, 'Height (in)', :class => 'col-md-2 control-label' %>
        <div class="col-md-2">
          <div class="checkbox">
            <%= check_box_tag :is_envelope %> Envelope
          </div>
        </div>
        <div class="col-md-8">
          <%= text_field_tag :height_in, '', :class => 'form-control' %>
        </div>
      </div>
      <div class="form-group">
        <%= label_tag :weight_lb, 'Weight (lb)', :class => 'col-md-2 control-label' %>
        <div class="col-md-10">
          <%= text_field_tag :weight_lb, '', :class => 'form-control' %>
        </div>
      </div>
    </div>

    <br />
    Please also upload photos of the package (for your protection, and for the receiver's peace of mind)<br />
    <br />

    <div class="col-md-12">
      <div class="form-group">
        <%= label_tag :photo_upload, 'Upload file', :class => 'col-md-2 control-label' %>
        <div class="col-md-10">
          <input type="file" name="photo_upload" class="form-control" />
        </div>
      </div>
    </div>

    <br />
    <p><em>You won't be able to update this information after submitting it, so make sure it's accurate.</em></p>

    <div class="buttons">
      <button type="submit" name="submit" value="submit" class="btn btn-primary">Submit info</button>
    </div>
  <% end %>
<% end %>
