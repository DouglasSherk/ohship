<% if @package.shippee_tracking.nil? %>
  <div class="alert alert-success">
    You've been assigned to forward this package! We'll notify you when the user actually orders the item.
  </div>
<% else %>
  The item is en route to you!
  <% if @package.shippee_tracking.empty? %>
    No tracking number was provided yet.
  <% else %>
    The tracking number is: <%= @package.shippee_tracking %> (<%= @package.shippee_tracking_carrier %>).
  <% end %>
  <br />
  Please notify us when the package reaches you.
  <%= form_tag package_path + '/shipper_action' do %>
    <div class="buttons">
      <button type="submit" name="submit" value="received" class="btn btn-primary">I received the package</button>
    </div>
  <% end %>
<% end %>
