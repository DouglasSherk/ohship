Hello <%= @user.name %>,

A person you referred (<%= @package.shippee.name %>) has sent a package with OhShip, so you get a referral credit!
You now have a total of <%= @user.referral_credits %> credit<%= @user.referral_credits == 1 ? '' : 's' %> you can use to ship packages with us without handling fees.

Thanks,
The OhShip Team
