Hello <%= @user.name %>,

We found a package that you can ship for us!
For package details, go to <%= package_url(@package) %>

We'll notify you when the user has actually shipped the package to you.

Regards,
The OhShip Team
